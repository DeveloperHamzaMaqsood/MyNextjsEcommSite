import Details from './prodDetails'

    const prodReducer  = (state = Details , action) => {
        switch(action.type) {
            default : return state;
        }
    }

    export default prodReducer;